# Beehiiv — credenciais usadas SOMENTE no servidor (app/api/subscribe/route.ts).
# Nunca versione o .env real nem exponha estas chaves no client.
# Sem estas variaveis, a rota /api/subscribe opera em modo mock (sucesso simulado),
# util para dev e para o preview da Vercel nao quebrar.

BEEHIIV_API_KEY=
BEEHIIV_PUBLICATION_ID=
